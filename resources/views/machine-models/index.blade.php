<x-layouts.app title="Machine Models">
    <div class="mb-6 flex items-center justify-between"><div><h1 class="app-page-title">{{ auth()->user()->isPlatformAdmin() ? 'Master Library' : 'Machine Models' }}</h1><p class="mt-1 text-sm text-slate-500">{{ auth()->user()->isPlatformAdmin() ? 'Create reusable makes, models and parser templates for all companies.' : 'Use your own models plus platform prebuilt models.' }}</p></div><a href="{{ route('machine-models.create') }}" class="app-button">Add model</a></div>
    <div class="app-panel app-table-wrap"><table class="app-table"><tbody>@foreach($machineModels as $model)<tr><td><a class="font-bold text-slate-950 hover:text-teal-700" href="{{ route('machine-models.show', $model) }}">{{ $model->manufacturer }} {{ $model->model_name }}</a><div class="text-xs text-slate-500">{{ $model->parser_type }} / {{ $model->company?->name ?? 'Platform master' }}</div></td>@unless(auth()->user()->isPlatformAdmin())<td>{{ $model->machines_count }} machines</td>@endunless<td>{{ $model->report_templates_count }} templates</td><td>@if(is_null($model->company_id))<span class="rounded-full bg-teal-50 px-3 py-1 text-xs font-bold text-teal-700">Prebuilt</span>@else<span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">Company</span>@endif</td></tr>@endforeach</tbody></table></div>
    <div class="mt-4">{{ $machineModels->links() }}</div>
</x-layouts.app>
